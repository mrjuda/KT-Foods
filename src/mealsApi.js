class mealAPI {
  meallikes = {
    mealsURL: '',
    passOpion: (objOption) => {
      const options = {
        method: `${obhOption}`,
        headers: {
          'Content-Type': 'application/json; charset=UTF-8',
        },
      };
      return options;
    },
    base
  }
}